# 会员端公告查看功能说明

## 功能概述
为会员端新增公告查看功能，会员可以查看所有已发布的公告，并查看公告详情。

## 实现内容

### 1. 新增文件
- **文件**: `src/views/MemberAnnouncementView.vue`
- **功能**: 会员公告查看页面

### 2. 路由配置
- **文件**: `src/router/index.ts`
- **新增路由**: `/member/announcements`
- **权限**: 需要登录（MEMBER角色）

### 3. 会员首页入口
- **文件**: `src/views/MemberHomeView.vue`
- **位置**: 快捷操作区域新增"查看公告"按钮
- **图标**: Bell（铃铛）

## 功能特性

### 公告列表页面
1. **公告列表展示**
   - 显示所有已发布的公告（publishStatus = 1）
   - 显示公告标题、优先级标签、内容预览
   - 显示发布时间、过期时间、门店信息
   - 支持点击公告卡片查看详情

2. **优先级标识**
   - 普通（蓝色）
   - 重要（橙色）
   - 紧急（红色）

3. **状态标识**
   - 已发布（绿色）
   - 草稿（灰色）

### 公告详情对话框
1. **详细信息展示**
   - 公告标题
   - 优先级和发布状态标签
   - 完整的发布时间、过期时间
   - 门店信息
   - 公告完整内容（支持换行显示）
   - 备注信息

2. **交互功能**
   - 点击列表中的任意公告卡片打开详情
   - 支持关闭对话框返回列表
   - 点击"返回首页"按钮返回会员首页

## API 接口调用

### 1. 获取公告列表
```typescript
GET /api/admin/announcements
参数:
  - page: 1
  - size: 50
  - publishStatus: 1 (只获取已发布的公告)
```

### 2. 获取公告详情
```typescript
GET /api/admin/announcements/:id
参数:
  - id: 公告ID
```

## 数据解析逻辑

代码兼容多种后端响应格式：
1. 直接返回数组
2. 标准格式 `{ code: 200, data: [...] }`
3. 分页格式 `{ code: 200, data: { records: [...] } }` 或 `{ list: [...] }`

## 测试步骤

### 1. 访问公告列表
1. 登录会员账号
2. 访问会员首页 `http://localhost:5173/member/home`
3. 点击"快捷操作"区域的"查看公告"按钮
4. 应该能看到公告列表页面

### 2. 查看公告详情
1. 在公告列表中点击任意公告卡片
2. 应该弹出公告详情对话框
3. 查看公告的完整信息

### 3. 返回首页
1. 点击页面右上角的"返回首页"按钮
2. 应该返回到会员首页

### 4. 游客模式测试
1. 退出登录
2. 访问会员首页
3. 点击"查看公告"按钮
4. 应该提示"请先登录"并跳转到登录页面

## 注意事项

1. **接口路径**: 使用的是 `/api/admin/announcements`，与管理员端共用同一个接口
2. **数据过滤**: 前端通过 `publishStatus: 1` 参数只获取已发布的公告
3. **内容格式化**: 公告内容中的换行符会被转换为HTML换行显示
4. **日期格式化**: 使用本地化日期格式显示（YYYY-MM-DD HH:mm）

## 样式特点

1. **响应式设计**: 支持不同屏幕尺寸
2. **悬停效果**: 公告卡片悬停时有提升和阴影效果
3. **清晰的层次**: 通过颜色、图标和布局区分不同信息
4. **友好的交互**: 点击卡片即可查看，操作直观

## 文件清单

- ✅ `src/views/MemberAnnouncementView.vue` - 会员公告查看页面
- ✅ `src/router/index.ts` - 路由配置（新增公告路由）
- ✅ `src/views/MemberHomeView.vue` - 会员首页（新增查看公告入口）
