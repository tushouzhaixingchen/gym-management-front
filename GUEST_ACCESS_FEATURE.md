# 游客访问功能说明

## 功能概述
实现了 `/member/home` 页面的游客访问模式，允许未登录用户预览会员首页，但限制具体功能的使用。

## 实现内容

### 1. 路由配置修改
- **文件**: `src/router/index.ts`
- **修改**: 将 `/member/home` 路由的 `requiresAuth` 从 `true` 改为 `false`
- **效果**: 允许未登录用户直接访问会员首页

### 2. 会员首页游客模式
- **文件**: `src/views/MemberHomeView.vue`

#### 主要变更：

**欢迎区域**
- 游客模式显示："欢迎游客访问！"
- 登录模式显示："欢迎回来，[用户名]！"
- 提示文字根据登录状态动态变化

**按钮变化**
- 游客模式：显示蓝色"登录"按钮
- 登录模式：显示红色"退出登录"按钮

**统计数据**
- 游客模式：所有数据显示为 0
  - 剩余课程：0
  - 优惠券：0
  - 入场次数：0
  - 当前等级：未登录
- 登录模式：显示实际数据

**今日课程**
- 游客模式：显示"无"（使用 `el-empty` 组件）
- 登录模式：显示课程列表

**功能按钮拦截**
所有功能按钮（预约课程、购买卡券、扫码入场、联系客服、个人信息、修改密码、我的预约）在游客模式下点击时：
1. 弹出提示："请先登录"
2. 自动跳转到登录页面

### 3. 核心代码逻辑

```typescript
// 判断是否为游客模式
const isGuest = computed(() => !userStore.token)

// 统一登录验证函数
const requireLogin = () => {
  if (isGuest.value) {
    ElMessage.warning('请先登录')
    router.push('/login')
    return true
  }
  return false
}
```

## 测试步骤

1. **清除登录状态**
   - 打开浏览器开发者工具
   - 清除 localStorage 中的 token
   - 或直接访问：`http://localhost:5173/member/home`

2. **验证游客模式**
   - ✅ 页面可以正常访问，无需登录
   - ✅ 显示"欢迎游客访问！"
   - ✅ 显示"登录"按钮（蓝色）
   - ✅ 剩余课程、优惠券等数据显示为 0
   - ✅ 今日课程显示"无"

3. **验证功能拦截**
   - 点击任意功能按钮（如"预约课程"）
   - ✅ 弹出提示"请先登录"
   - ✅ 自动跳转到登录页面

4. **验证登录后状态**
   - 登录后重新访问 `/member/home`
   - ✅ 显示用户名称
   - ✅ 显示"退出登录"按钮（红色）
   - ✅ 显示实际统计数据
   - ✅ 显示今日课程列表
   - ✅ 功能按钮可正常使用

## 注意事项

1. 如果修改后页面仍显示旧内容，请执行硬刷新（Ctrl+Shift+R 或 Cmd+Shift+R）
2. 游客模式的数据目前为静态值，后续可从接口获取真实的统计数据
3. 所有需要登录的功能都统一使用 `requireLogin()` 函数进行拦截

## 文件清单

- ✅ `src/router/index.ts` - 路由配置
- ✅ `src/views/MemberHomeView.vue` - 会员首页组件
